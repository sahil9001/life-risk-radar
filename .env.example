# Copy to .env.local and restart `npm run dev`.
# When set, the "Ask your life" box answers ANY free-text question by having
# Claude generate Coral SQL on the fly (validated read-only) and summarize the
# results. Without it, the four built-in questions still work via vetted SQL.
ANTHROPIC_API_KEY=sk-ant-...
